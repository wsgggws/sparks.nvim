# sparks.nvim 0.4.0

Sparks becomes a general visual-feedback engine. Other plugins can call `emit()`, users can
register data-driven particle effects, and optional integrations celebrate saves, cleared
errors, and test-success User events. Rejections return stable reason strings so callers can
decide whether to retry or remain quiet.
